name: ipa
on: [push]
jobs:
  build:
    runs-on: macos-latest
    strategy:
      matrix:
        platform:
          - iOS
    steps:
      - uses: actions/checkout@v1
      - uses: mxcl/xcodebuild@v3
        with:
          working-directory: ./ios/app
