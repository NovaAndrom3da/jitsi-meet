name: ipa
on: [push]
jobs:
  build:
    runs-on: macos-latest
    strategy:
      matrix:
        platform:
          - iOS
    steps:
      - uses: mxcl/xcodebuild@v3
        with:
          working-directory: ios/app
